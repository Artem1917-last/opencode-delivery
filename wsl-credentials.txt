# WSL2 Debian Credentials
# Created for OpenCode Delivery project

## WSL2 Debian User
Username: devuser
Password: packer2026

## Notes
- This is for WSL2 environment only (Ubuntu/Debian inside Windows)
- NOT used for Packer VM (Packer uses its own SSH credentials)
- NOT used for OpenCode server (those are in .env)
- Used for running QEMU + KVM from within WSL2

## Commands
wsl -d Debian          # Start Debian WSL2
wsl -l -v              # Check WSL2 status
wsl --shutdown         # Stop all WSL2 instances
